module.exports = {
    darkMode: false, // Desativa o modo escuro globalmente
    theme: {
      extend: {
        // Aqui você pode personalizar as cores ou outros estilos, se necessário
      },
    },
    plugins: [
      require("daisyui"), // DaisyUI será utilizado como plugin
    ],
  }